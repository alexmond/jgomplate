name: jgomplate
title: jgomplate
version: true
start_page: ROOT:index.adoc
asciidoc:
  attributes:
    # javadoc.io serves versioned API docs for every published artifact; the /page/ form
    # keeps the javadoc.io chrome + version switcher. Compose as
    # {api-core}/{page-component-version}/<class path>.html in pages — the component
    # version is derived by Antora from the tag the hub pins, so it needs no manual bump.
    api-functions: 'https://javadoc.io/page/org.alexmond/jgomplate-functions'
    api-core: 'https://javadoc.io/page/org.alexmond/jgomplate-core'
    toc: left
    sectnums: ''
    keywords: 'gomplate, Go template, text/template, Sprig, Java'
    author: 'Alexander Mondshain'
    email: 'alex.mondshain@gmail.com'
nav:
  - modules/ROOT/nav.adoc
