# The CLI's stdout is the rendered template — keep Spring's banner and startup logging
# off so nothing but the render (and real errors) reaches the shell.
spring:
  main:
    banner-mode: "off"
    log-startup-info: false
logging:
  level:
    root: warn
